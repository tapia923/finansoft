@model IEnumerable<PlanillajeColectivos.DTO.EntidadDos.cuposCredito>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>pruebaConexionDos</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cupo)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cupo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.NIT }) |
                @Html.ActionLink("Details", "Details", new { id=item.NIT }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.NIT })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
